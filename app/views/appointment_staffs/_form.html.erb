<%= form_with(model: appointment_staff, local: true) do |form| %>
  <% if appointment_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appointment_staff.errors.count, "error") %> prohibited this appointment_staff from being saved:</h2>

      <ul>
      <% appointment_staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
